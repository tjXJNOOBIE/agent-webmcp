(()=>{const toast=document.querySelector('.toast');let timer;const show=(message)=>{if(!toast)return;toast.textContent=message;toast.classList.add('show');clearTimeout(timer);timer=setTimeout(()=>toast.classList.remove('show'),1600)};document.addEventListener('click',event=>{const action=event.target.closest('[data-action]');if(action){event.preventDefault();show(action.dataset.action)}const selectable=event.target.closest('[data-select-group]');if(selectable){const group=selectable.dataset.selectGroup;document.querySelectorAll(`[data-select-group="${group}"]`).forEach(el=>el.classList.remove('selected','active'));selectable.classList.add(selectable.matches('.service-nav-item,.side-tab,.c-target-tab')?'active':'selected')}});document.querySelectorAll('[data-filter-input]').forEach(input=>{const selector=input.dataset.filterInput;input.addEventListener('input',()=>{const query=input.value.trim().toLowerCase();document.querySelectorAll(selector).forEach(node=>{node.style.display=!query||node.textContent.toLowerCase().includes(query)?'':'none'})})});document.addEventListener('keydown',event=>{if(event.key==='/'&&!/input|textarea/i.test(document.activeElement?.tagName||'')){const search=document.querySelector('[data-primary-search]');if(search){event.preventDefault();search.focus()}}});})();
